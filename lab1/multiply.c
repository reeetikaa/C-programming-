// Write a program to multiply two numbers (10&8) and display its product.

#include<stdio.h>
int main(){
    int a = 10, b = 8, multiply;
    multiply = a*b;
    printf("The multiple of 10 and 8 is :%d",multiply);
    return 0;
}